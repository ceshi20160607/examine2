# FE-018 运行台记录真实 UI

## 范围

P10 补齐运行台菜单、模块 schema、记录查询、新建、详情、编辑、提交和历史页面能力，入口位于系统内 `运行台 / 运行模块`。

## 页面能力

| 能力 | API | 验证结果 |
| --- | --- | --- |
| 运行台菜单 | RUN-001 | 页面可进入运行台模块 |
| 模块 schema | RUN-002 | 浏览器深链路自动 SYS-001 后加载 schema |
| 记录查询 | RUN-003 | 已修复 POST body，查询通过 |
| 新建记录 | RUN-004 | 已创建记录 `2064251596901019649` 和 `2064260752819183618` |
| 详情 | RUN-005 | 已查看动态字段值 |
| 编辑保存 | RUN-006 | 已补幂等 key，编辑保存通过，版本升到 `2` |
| 提交 | RUN-008 | 已补幂等 key，提交通过，状态 `SUBMITTED`、版本 `3` |
| 历史 | RUN-009 | 已查看 CREATE、UPDATE、SUBMIT 历史 |

## 修复点

1. 前端运行态 schema 兼容后端 `code/name` 字段，统一映射为 `fieldCode/fieldName`。
2. 系统内深链路会自动调用 SYS-001 建立系统、租户、成员和权限上下文。
3. RUN-003 改为 POST body。
4. RUN-006、RUN-008 补齐幂等 key。
5. 后端记录标题在未配置标题字段时使用第一个有文本的动态字段值兜底。

## 结论

FE-018 通过。运行台记录链路已通过真实浏览器 E2E。
