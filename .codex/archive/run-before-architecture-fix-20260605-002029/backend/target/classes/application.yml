server:
  port: 8080
spring:
  application:
    name: examine-backend
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:jdbc:mysql://192.168.0.211:3306/examine1?characterEncoding=utf8&useSSL=false&zeroDateTimeBehavior=convertToNull&tinyInt1isBit=false&serverTimezone=Asia/Shanghai&useAffectedRows=true&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:Admin001m}
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    banner: false
app:
  security:
    token-secret: ${APP_TOKEN_SECRET:change-me-token-secret}
    openapi-secret: ${APP_OPENAPI_SECRET:change-me-openapi-secret}
    token-ttl-minutes: 720
