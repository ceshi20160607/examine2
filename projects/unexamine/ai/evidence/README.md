# 周期证据

每个最多 240 分钟的开发周期结束时只保存该周期的受影响测试、组合验证、数据库读回和真实入口结果。单元测试通过、工程组合通过、浏览器业务旅程通过、用户验收分别记录，不互相替代。

需求阶段的 `requirement-fragment-audit.json` 是候选需求片段的语义审计证据。`accepted` 表示粒度和原文语义通过总控复核；`revise` 必须列出具体问题。每条结果绑定对应片段内容哈希，片段变动后必须重审。结构校验通过不能代替此审计。
