# 计划生成顺序

1. 合并全量需求分析，建立每个产品能力的唯一归属。
2. 为每个能力拆原子用例和验收条件。
3. 每个用例再拆成数据库、生成 `base`、后端 `manage`、前端、测试与运维任务；不需要的任务类型明确标记不适用。
4. 审计现有代码，逐项判定保留、补全、重做或删除；已有代码不能改变任务范围。
5. 建立依赖和资源冲突后，才把 10..120 分钟任务装入最多 240 分钟的周期。

阶段只表示依赖里程碑，一个能力只能有一个主阶段；成熟化、安全和性能是该能力的约束或验收任务，不能再次把整项能力排一遍。
