<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unique.unexamine.dataexchange.base.mapper.ExchangeImportRowMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.unique.unexamine.dataexchange.base.entity.ExchangeImportRow">
        <id column="id" property="id" />
        <result column="batch_id" property="batchId" />
        <result column="row_number" property="rowNumber" />
        <result column="raw_json" property="rawJson" />
        <result column="normalized_json" property="normalizedJson" />
        <result column="status" property="status" />
        <result column="target_record_id" property="targetRecordId" />
        <result column="error_json" property="errorJson" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="version" property="version" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, batch_id, `row_number`, raw_json, normalized_json, `status`, target_record_id, error_json, created_at, updated_at, version
    </sql>

</mapper>
