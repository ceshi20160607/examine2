<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unique.unexamine.application.base.mapper.AppGrantFieldMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.unique.unexamine.application.base.entity.AppGrantField">
        <id column="id" property="id" />
        <result column="grant_id" property="grantId" />
        <result column="field_code" property="fieldCode" />
        <result column="readable" property="readable" />
        <result column="writable" property="writable" />
        <result column="mask_strategy" property="maskStrategy" />
        <result column="created_at" property="createdAt" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, grant_id, field_code, readable, writable, mask_strategy, created_at
    </sql>

</mapper>
