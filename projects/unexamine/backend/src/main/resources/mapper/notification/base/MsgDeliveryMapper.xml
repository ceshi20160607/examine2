<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unique.unexamine.notification.base.mapper.MsgDeliveryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.unique.unexamine.notification.base.entity.MsgDelivery">
        <id column="id" property="id" />
        <result column="message_id" property="messageId" />
        <result column="account_id" property="accountId" />
        <result column="channel" property="channel" />
        <result column="destination_masked" property="destinationMasked" />
        <result column="status" property="status" />
        <result column="attempt_count" property="attemptCount" />
        <result column="next_attempt_at" property="nextAttemptAt" />
        <result column="provider_message_id" property="providerMessageId" />
        <result column="provider_receipt_json" property="providerReceiptJson" />
        <result column="last_error" property="lastError" />
        <result column="sent_at" property="sentAt" />
        <result column="delivered_at" property="deliveredAt" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="version" property="version" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, message_id, account_id, `channel`, destination_masked, `status`, attempt_count, next_attempt_at, provider_message_id, provider_receipt_json, last_error, sent_at, delivered_at, created_at, updated_at, version
    </sql>

</mapper>
