// Generated by MyBatis-Plus Generator via Template Base. Do not edit.
package com.unique.unexamine.ai.base.service;

import com.baomidou.mybatisplus.core.conditions.Wrapper;
import com.unique.unexamine.ai.base.entity.AiMessage;
import com.unique.unexamine.ai.base.mapper.AiMessageMapper;
import org.springframework.stereotype.Service;

import java.io.Serializable;
import java.util.List;

@Service
public class AiMessageBaseService {
    private final AiMessageMapper mapper;

    public AiMessageBaseService(AiMessageMapper mapper) {
        this.mapper = mapper;
    }

    public int insert(AiMessage entity) {
        return mapper.insert(entity);
    }

    public AiMessage selectById(Serializable id) {
        return mapper.selectById(id);
    }

    public List<AiMessage> selectList(Wrapper<AiMessage> query) {
        return mapper.selectList(query);
    }

    public int updateById(AiMessage entity) {
        return mapper.updateById(entity);
    }

    public int deleteById(Serializable id) {
        return mapper.deleteById(id);
    }
}
