// Generated by MyBatis-Plus Generator via Template Base. Do not edit.
package com.unique.unexamine.file.base.service;

import com.baomidou.mybatisplus.core.conditions.Wrapper;
import com.unique.unexamine.file.base.entity.FileStorageBackend;
import com.unique.unexamine.file.base.mapper.FileStorageBackendMapper;
import org.springframework.stereotype.Service;

import java.io.Serializable;
import java.util.List;

@Service
public class FileStorageBackendBaseService {
    private final FileStorageBackendMapper mapper;

    public FileStorageBackendBaseService(FileStorageBackendMapper mapper) {
        this.mapper = mapper;
    }

    public int insert(FileStorageBackend entity) {
        return mapper.insert(entity);
    }

    public FileStorageBackend selectById(Serializable id) {
        return mapper.selectById(id);
    }

    public List<FileStorageBackend> selectList(Wrapper<FileStorageBackend> query) {
        return mapper.selectList(query);
    }

    public int updateById(FileStorageBackend entity) {
        return mapper.updateById(entity);
    }

    public int deleteById(Serializable id) {
        return mapper.deleteById(id);
    }
}
