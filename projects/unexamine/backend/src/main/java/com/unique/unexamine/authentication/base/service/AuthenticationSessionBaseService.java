// Generated by MyBatis-Plus Generator via Template Base. Do not edit.
package com.unique.unexamine.authentication.base.service;

import com.baomidou.mybatisplus.core.conditions.Wrapper;
import com.unique.unexamine.authentication.base.entity.AuthenticationSession;
import com.unique.unexamine.authentication.base.mapper.AuthenticationSessionMapper;
import org.springframework.stereotype.Service;

import java.io.Serializable;
import java.util.List;

@Service
public class AuthenticationSessionBaseService {
    private final AuthenticationSessionMapper mapper;

    public AuthenticationSessionBaseService(AuthenticationSessionMapper mapper) {
        this.mapper = mapper;
    }

    public int insert(AuthenticationSession entity) {
        return mapper.insert(entity);
    }

    public AuthenticationSession selectById(Serializable id) {
        return mapper.selectById(id);
    }

    public List<AuthenticationSession> selectList(Wrapper<AuthenticationSession> query) {
        return mapper.selectList(query);
    }

    public int updateById(AuthenticationSession entity) {
        return mapper.updateById(entity);
    }

    public int deleteById(Serializable id) {
        return mapper.deleteById(id);
    }
}
