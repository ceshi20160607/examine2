// Generated by MyBatis-Plus Generator via Template Base. Do not edit.
package com.unique.unexamine.platform.base.service;

import com.baomidou.mybatisplus.core.conditions.Wrapper;
import com.unique.unexamine.platform.base.entity.PlatSsoIdentity;
import com.unique.unexamine.platform.base.mapper.PlatSsoIdentityMapper;
import org.springframework.stereotype.Service;

import java.io.Serializable;
import java.util.List;

@Service
public class PlatSsoIdentityBaseService {
    private final PlatSsoIdentityMapper mapper;

    public PlatSsoIdentityBaseService(PlatSsoIdentityMapper mapper) {
        this.mapper = mapper;
    }

    public int insert(PlatSsoIdentity entity) {
        return mapper.insert(entity);
    }

    public PlatSsoIdentity selectById(Serializable id) {
        return mapper.selectById(id);
    }

    public List<PlatSsoIdentity> selectList(Wrapper<PlatSsoIdentity> query) {
        return mapper.selectList(query);
    }

    public int updateById(PlatSsoIdentity entity) {
        return mapper.updateById(entity);
    }

    public int deleteById(Serializable id) {
        return mapper.deleteById(id);
    }
}
