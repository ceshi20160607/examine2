// Generated by MyBatis-Plus Generator via Template Base. Do not edit.
package com.unique.unexamine.analytics.base.service;

import com.baomidou.mybatisplus.core.conditions.Wrapper;
import com.unique.unexamine.analytics.base.entity.AnaDashboard;
import com.unique.unexamine.analytics.base.mapper.AnaDashboardMapper;
import org.springframework.stereotype.Service;

import java.io.Serializable;
import java.util.List;

@Service
public class AnaDashboardBaseService {
    private final AnaDashboardMapper mapper;

    public AnaDashboardBaseService(AnaDashboardMapper mapper) {
        this.mapper = mapper;
    }

    public int insert(AnaDashboard entity) {
        return mapper.insert(entity);
    }

    public AnaDashboard selectById(Serializable id) {
        return mapper.selectById(id);
    }

    public List<AnaDashboard> selectList(Wrapper<AnaDashboard> query) {
        return mapper.selectList(query);
    }

    public int updateById(AnaDashboard entity) {
        return mapper.updateById(entity);
    }

    public int deleteById(Serializable id) {
        return mapper.deleteById(id);
    }
}
