# 需求基线

本目录存放由 `docs/user_requirement.md` 确定性生成的需求索引、分析分包、原子需求与验收用例。

- `requirement-intake.json` 只记录源区块、行号和哈希，不改写用户需求。
- 后续分析结果必须覆盖每个可覆盖区块，并绑定到当前需求哈希。
- 需求文件发生变化后，旧索引和旧分析立即失效，必须显式重新生成。
- `slice-01.scope.json` 只选择第一条纵向切片必需的标题路径；未选区块保留为可统计的 deferred，不等于删除需求。
