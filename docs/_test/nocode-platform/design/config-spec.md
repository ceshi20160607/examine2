# config-spec（测试 L3 · 节选）

## 1. P-RUNTIME-LIST 请假列表 ★

| 区域 | 控件 | 权限 | 显隐 |
|------|------|------|------|
| 页头 | 新建请假 | create | 常显 |
| 工具栏 | 搜索 | — | 常显 |
| 工具栏 | 场景下拉 | — | 全部/我发起的/… |
| 工具栏 | 高级筛选 | — | 抽屉 |
| 工具栏 | 列设置 | — | 抽屉 |
| 工具栏 | 导出（全部） | export | |
| 工具栏 | 导入 | import | P1 占位 |
| 选中条 | 批量删除/导出选中 | delete/export | 勾选后 |
| 表 | 序号+勾选 | — | |
| 行 | 点击 | view | 右侧详情抽屉 |

密度：1440×900 首屏 ≥8 行（seed 数据）。

## 2. P-SYS-ADMIN-MODELING ★

| Tab | 内容 |
|-----|------|
| 字段 | 表格+右侧抽屉（类型/字典/必填/列表显隐） |
| 列表 | 默认列、排序 |
| 筛选 | 可筛字段 |
| 场景 | 预设场景 |
| 动作 | 新建/导出/导入/删除 |
| 菜单发布 | 绑定模块组 |

顶栏：保存草稿 | **发布**（主）

## 3. P-PLAT-ADMIN-API-CONSOLE ★（增量）

| 区域 | 控件 |
|------|------|
| 左 | API 产品列表 |
| 中 | 路由/模块绑定、版本 |
| 右 | 凭证 scope、**字段映射表**、限流、IP 白名单 |
| 底 | 调用日志入口 |

映射表列：外部参数 | 内部字段 | 转换 | 必填

## 4. P-PLAT-ADMIN-SSO

| 字段 | 说明 |
|------|------|
| IdP 类型 | OIDC / SAML(占位) |
| Issuer、Client ID | 表单 |
| 回调 URL | 只读复制 |
| 测试连接 | 按钮 |
| 强制 SSO | 开关 |

## 5. P-SYS-ADMIN-TENANT

| 字段 | 说明 |
|------|------|
| 模式 | 单租户 / 多租户 单选 |
| 默认租户 | 下拉 |
| 租户列表 | 名称、编码、状态（多租户时） |

其余 P0 页见 examine2 `page-inventory` 同构 + 上表增量。
