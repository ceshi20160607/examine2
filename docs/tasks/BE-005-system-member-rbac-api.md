# BE-005 系统成员 RBAC API

- taskId: BE-005
- 标题: 系统成员 RBAC API
- 负责角色: backend
- 所属大任务/模块: 后端 / 系统管理与权限
- 目标: 实现系统上下文、租户、成员、部门、角色、权限目录和有效权限接口。
- 输入文件: `docs/api.md`、`docs/db_design.md`、`backend/examine-core/`
- 输出文件或输出目录: `backend/examine-module/`、`backend/examine-plat/`

## 详细工作内容

- 实现 SYS、MEM、RBAC 接口组。
- 维护成员扩展、部门树、角色授权、权限目录、有效权限和运行菜单。
- 确保成员、角色、租户、系统状态参与权限校验。

## 完成状态定义

- 默认状态: pending。
- 完成条件: 系统管理和权限接口符合冻结契约。

## 验收标准

- 平台账号进入系统后转换为系统成员上下文。
- 角色授权支持菜单、操作、字段、数据范围、导出、流程、OpenAPI scope。

## 测试/自检要求

- 测试跨系统访问、成员停用、角色停用、字段无写权、数据范围越权。

## 依赖任务

- BE-004
- BE-014

## 可并行关系

- 完成后启动 BE-006；BE-007 需等待 BE-006 完成。

## 不允许事项

- 不让系统管理员管理平台全局配置。
- 不跳过后端权限校验依赖前端过滤。
