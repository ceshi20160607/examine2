# FE-002 路由布局认证上下文

- taskId: FE-002
- 标题: 路由布局认证上下文
- 负责角色: frontend
- 所属大任务/模块: 前端 / 应用骨架
- 目标: 建立前端路由、布局、认证态、系统/租户/member 上下文和权限守卫。
- 输入文件: `docs/api.md`、`frontend/src/api/`
- 输出文件或输出目录: `frontend/src/router/`、`frontend/src/layouts/`、`frontend/src/stores/`、`frontend/docs/page-contracts/FE-002-routing-layout-auth-context.md`

## 详细工作内容

- 配置登录区、平台中心、系统管理、应用配置、运行台、流程、文件导出、OpenAPI、审计运维路由。
- 实现 token、当前用户、当前系统、当前租户、当前成员和有效权限状态管理。
- 实现 requestId 错误展示和统一错误处理入口。
- 记录路由、布局、认证上下文和统一错误处理的页面级契约证据。

## 完成状态定义

- 默认状态: pending。
- 完成条件: 页面模块可按路由挂载并共享上下文，`FE-002` 页面级契约证据文件已补齐。

## 验收标准

- 未登录访问内部页面会跳转登录。
- 切换系统或租户后刷新权限和菜单。

## 测试/自检要求

- 自检路由守卫、上下文刷新、系统停用提示。

## 依赖任务

- FE-001

## 可并行关系

- 不可并行；模块页面依赖本任务。

## 不允许事项

- 不在页面组件里重复实现鉴权分支。
- 不持久化敏感 token 明文到不安全位置。
