# 前端新 PRD 复审

## 结论

**status: pass**

本次仅按“新 PRD 复审”范围读取 `docs/user_requirement.md`、`docs/requirement_analysis.md`、`docs/legacy_reference.md`、`docs/service_info.md`、`docs/project_understanding.md`、`docs/prd.md`，未读取其它输入，未写入前端代码。

复核结论：`docs/prd.md` 已满足 `docs/project_understanding.md` 中 PU-001 至 PU-018 的前端相关关闭条件。页面入口、角色入口、导航分区、工作台、页面清单、列表字段、筛选、表单/详情、按钮动作、状态矩阵、空态/加载态/错误态、权限禁用态、保存刷新规则、移动端 MVP 边界和 typed SDK 前置契约均已在 PRD 阶段达到可进入下一轮角色复审的粒度。

## 重点项复核

| 复核项 | 结论 | PRD 覆盖情况 |
| --- | --- | --- |
| 角色入口/导航 | pass | `用户角色` 表明确平台超级管理员、平台用户、系统超级管理员、系统管理员、租户管理员、应用配置管理员、业务用户、审批人、运维审计人员、OpenAPI 客户端的典型入口；`模块范围矩阵` 和 `页面/交互矩阵` 进一步拆分平台中心、系统管理、应用配置、运行台、流程工作台、文件/导出、OpenAPI、运维审计入口。 |
| 工作台 | pass | PRD 明确“我的系统”作为登录后默认入口，应用运行台承接普通业务用户，流程工作台承接审批人待办/抄送/我的申请，运维审计中心承接只读排查。 |
| 页面清单 | pass | `页面/交互矩阵` 已列出登录/注册、我的系统、平台账号/角色、系统基础信息、租户/成员/部门、角色权限、应用列表/概览、模块/字段设计、页面/菜单/权限配置、业务列表、业务表单/详情、流程待办、文件/导出、OpenAPI、运维审计等首期页面。 |
| 列表字段/筛选 | pass | 每个列表型页面均给出列表字段和筛选项；通用规则要求服务端分页、排序和筛选，前端不可一次性加载全量业务记录。 |
| 表单字段/详情区块 | pass | 关键页面已给出表单或详情字段范围，如系统基础信息、成员扩展详情、角色授权、字段设计、页面配置、业务表单详情、审批详情、文件引用关系、OpenAPI 客户端详情和日志详情。 |
| 按钮动作/按钮状态 | pass | 页面矩阵列出主要按钮；状态矩阵和通用交互规则明确保存、发布、审批、导出创建、凭证轮换等请求中禁用，权限或业务状态不满足时过滤或禁用。 |
| 状态矩阵 | pass | `数据规则/状态矩阵` 覆盖账号、系统、租户、应用、模块、字段、配置版本、业务记录、流程模板、流程实例、流程任务、文件、导出任务、OpenAPI 客户端。 |
| 空态/加载态/错误态 | pass | 页面矩阵覆盖无系统、无账号、无成员、无角色、无应用、无字段、无页面、无数据、无待办、无客户端、日志为空等空态；错误态要求 requestId、字段定位、失败原因、重试入口；加载失败在页面矩阵中明确，具体骨架或 spinner 可在前端设计阶段细化，不阻塞 PRD 关闭。 |
| 权限禁用态 | pass | 页面矩阵给出只读、隐藏、禁用、禁止下载、非候选人禁用处理按钮等前端表现；权限规则明确菜单、按钮、字段、数据范围、导出和 OpenAPI scope，且前端权限仅作体验过滤，后端仍需校验。 |
| 保存刷新规则 | pass | 页面矩阵逐页说明保存后刷新当前页、详情抽屉、系统上下文、树/列表/人员选择器缓存、权限缓存、侧边导航、schema 缓存、待办状态和结果文件。 |
| 移动端边界 | pass | 通用交互规则明确 MVP 移动端只覆盖登录、运行台列表/详情轻量查看、表单轻量编辑、附件拍照/上传和待办处理，不提供复杂配置器、批量导入导出和高级运维页面。 |
| typed SDK 前置契约 | pass | `前端执行边界` 明确前端实现前必须完成 API 契约冻结和 typed SDK 生成，不得散落手写 fetch/axios 调用；页面到接口映射、上下文字段自动补齐、枚举、错误码、分页、排序、筛选、动态字段值结构必须在 API 阶段闭环。 |

## PU 关闭条件复核

| PU | 前端相关性 | 结论 | 复核说明 |
| --- | --- | --- | --- |
| PU-001 | 低 | pass | PRD 背景和验收标准明确当前活动区重建原则，不沿用归档 PRD/API/DB/SQL/代码，避免前端复用旧调试页口径。 |
| PU-002 | 低 | pass | PRD 明确 `un_plat_`，未发现 `un_platt_`；该项主要影响 DB/API，前端无阻塞。 |
| PU-003 | 低 | pass | PRD 明确 OpenAPI 表前缀为 `un_openapi_`，`examine-app` 为后端模块名；前端 OpenAPI 页面归属清晰。 |
| PU-004 | 中 | pass | PRD 明确平台用户、系统成员、租户和系统内权限边界，用户角色和权限规则可支撑前端路由与上下文切换。 |
| PU-005 | 中 | pass | 功能范围按 MVP、后续增强、暂不做拆分，移动端完整适配、复杂 BI、复杂页面设计器等未压入 MVP。 |
| PU-006 | 中 | pass | 业务流程和数据规则明确页面、字段、流程、导出配置的草稿/发布/版本语义，运行台读取发布版本。 |
| PU-007 | 中 | pass | 数据规则明确 record、value、index、relation、subtable、history 产品边界，前端动态字段展示和历史解释有方向。 |
| PU-008 | 高 | pass | 状态矩阵覆盖核心对象，并与页面按钮禁用、流程锁定、系统停用、凭证过期等页面反馈衔接。 |
| PU-009 | 高 | pass | 权限规则、权限矩阵和页面权限禁用态覆盖菜单、操作、字段、数据范围、导出、OpenAPI scope；可支撑前端菜单过滤和按钮/字段禁用。 |
| PU-010 | 中 | pass | 业务记录保存、提交审批、导出任务、OpenAPI 调用流程明确事务、状态影响和失败页面反馈。 |
| PU-011 | 中 | pass | 文件状态、引用、删除、下载/预览权限和上传/导出失败反馈已在业务流程、数据规则和页面矩阵覆盖。 |
| PU-012 | 中 | pass | PRD 明确 MVP 做导出闭环，导入执行延后；页面矩阵保留文件/导出入口和导出任务状态，不要求前端伪造导入闭环。 |
| PU-013 | 中 | pass | OpenAPI 客户端、凭证、scope、IP 白名单、日志、状态和错误反馈明确，前端客户端管理页和日志页有页面闭环。 |
| PU-014 | 高 | pass | PRD 明确 requestId 贯穿前端错误、后端日志、导出任务和 OpenAPI 调用日志，并定义错误码命名空间；具体错误码待 API 阶段冻结。 |
| PU-015 | 高 | pass | 页面/交互矩阵已按页面列入口、字段、筛选、按钮、状态反馈、权限禁用和保存刷新规则，满足页面级矩阵关闭条件。 |
| PU-016 | 高 | pass | PRD 明确 API 契约冻结与 typed SDK 为前端实现前置条件，并要求 API 阶段闭环页面到接口映射、上下文字段、枚举、错误码、分页排序筛选和动态字段值结构。 |
| PU-017 | 低 | pass | 该项主要由 test/DBA 复核；PRD 已明确默认平台管理员、角色、菜单、默认系统/租户/应用/字段类型元数据方向，前端无额外阻塞。 |
| PU-018 | 低 | pass | 该项主要由 analyst/DBA 处理；PRD 已标明不阻塞 PRD、但阻塞 DB 设计。旧项目前端无源码可沿用，前端无额外关闭条件。 |

## Issue 表

无。当前结论为 pass，不输出 fail issue 表。

