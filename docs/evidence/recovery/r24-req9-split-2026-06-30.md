# R24 REQ-9 Split Evidence

Date: 2026-06-30

Task: `REC-P0-027 FRC-1 Missing Product Surfaces Closure`

Scope:

`REQ-9` was too broad to implement or accept as one task. It is now split into six explicit task cards:

- `REC-P0-028 Command Center` for `REQ-9.1`.
- `REC-P0-029 Right-Side Intelligent Assistant` for `REQ-9.2`.
- `REC-P0-030 Schema-Driven Page Runtime` for `REQ-9.3`.
- `REC-P0-031 Advanced Table` for `REQ-9.4`.
- `REC-P0-032 Flow Simulation` for `REQ-9.5`.
- `REC-P0-033 Print Template Visual Designer` for `REQ-9.6`.

Boundary:

This moves `REQ-9` from `OPEN` to `PARTIAL`, not `PROVEN`.

The split is valid recovery progress because each card now has:

- role and business outcome,
- frontend scope,
- backend coded behavior,
- generated-code boundary,
- data scope,
- permission rules,
- explicit non-completion cases,
- acceptance script.

Still required:

- Implement each split task.
- Add deployed browser/API/database evidence for each task.
- Update the final requirement ledger from `PARTIAL` only when evidence scope matches.
