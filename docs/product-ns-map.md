# 产品全量功能 NS 图（超级管理员视角）

> 这份图是给人看的产品功能地图，不是研发排期图。
> 默认按“超级管理员全量功能”展示；实际用户看到什么，由角色权限、菜单权限、按钮权限、字段权限、数据权限裁剪。

## 0. 读图规则

- **全量视角**：默认超级管理员可看到平台后台、系统后台、系统运行态全部入口。
- **普通成员视角**：例如 `che`，只看到车系统运行态和被授权的业务模块。
- **系统管理员视角**：例如 `admin`，看到车系统运行态 + 系统后台，但看不到平台后台。
- **平台管理员视角**：例如 `plat_admin`，看到平台工作台 + 平台后台，也可以进入已授权系统。
- **配置驱动**：页面菜单、按钮、列表字段、表单字段、详情字段、批量操作、导出能力都从配置和权限来。

## 1. 登录后按角色分流

```mermaid
flowchart TD
    A["打开 unexamine"] --> B{"是否已有账号"}
    B -->|没有账号| C["注册并创建系统"]
    C --> C1["创建平台用户"]
    C1 --> C2["创建业务系统"]
    C2 --> C3["注册人成为该系统超级管理员"]
    C3 --> H_SYS_ADMIN["进入系统后台首页"]

    B -->|已有账号| D["登录"]
    D --> E["平台工作台"]
    E --> E_TOP["顶栏：待办 / 消息 / 用户菜单"]
    E --> E_SIDE["侧栏：我的系统 / Flow / 应用 / 日志"]
    E --> F{"当前账号权限"}

    F -->|平台超级管理员 plat_admin| P_FULL["平台全量视角"]
    P_FULL --> P_WORK["平台工作台"]
    P_FULL --> P_ADMIN["平台后台"]
    P_FULL --> S_ENTRY["进入已授权系统"]

    F -->|系统管理员 admin| S_ADMIN_ROLE["系统管理员视角"]
    S_ADMIN_ROLE --> S_RUNTIME_ADMIN["车系统运行态"]
    S_ADMIN_ROLE --> H_SYS_ADMIN

    F -->|普通成员 che| S_MEMBER_ROLE["普通成员视角"]
    S_MEMBER_ROLE --> S_RUNTIME_CHE["车系统运行态"]
    S_MEMBER_ROLE -. "不可见" .-> NO_SYS_ADMIN["系统后台"]
    S_MEMBER_ROLE -. "不可见" .-> NO_PLAT_ADMIN["平台后台"]

    S_ENTRY --> S_RUNTIME_ADMIN
    H_SYS_ADMIN --> S_PUBLISH["配置并发布"]
    S_PUBLISH --> S_RUNTIME_ADMIN
    S_RUNTIME_CHE --> BIZ["业务使用：车辆档案 / 维保记录 / 驾驶员 / 审批"]
    S_RUNTIME_ADMIN --> BIZ
```

## 2. 超级管理员全量功能地图

```mermaid
mindmap
  root((超级管理员全量功能))
    登录与账号
      登录
      退出
      注册并创建系统
      平台用户唯一认证
    平台工作台
      我的系统
        系统列表
        最近访问
        进入车系统
      顶栏待办
        跨系统待办
        审批提醒
      顶栏消息
        系统通知
        审批消息
      Flow
        平台 Flow 工作台
        跨系统流程入口
      应用
        平台应用入口
        平台能力聚合
      日志
        平台工作台日志
    平台后台
      概览
        平台用户数
        系统数
        Flow 数
        应用数
        调用统计
      平台用户
        新建用户
        启用停用
        搜索筛选
        最近登录
      平台角色
        平台菜单权限
        平台用户管理权限
        平台 Flow 权限
        平台应用权限
        平台日志权限
      平台 Flow
        Flow 列表
        触发方式
        版本发布
        调用情况
        授权给平台应用
      平台应用
        AppKey 脱敏
        Scope 授权
        绑定平台 Flow
        参数映射
        凭证轮换
        调用日志
      平台日志
        操作人
        对象
        类型
        来源
        结果
      全局配置
        登录安全
        OpenAPI 策略
        文件存储
        系统配额
        功能开关
    车系统运行态
      仪表盘
        车辆总数
        在用车辆
        待年检
        维保中
        快捷入口
      车辆管理
        车辆档案
        维保记录
        驾驶员
      车辆档案列表
        场景
        搜索
        高级筛选
        列设置
        导入
        全部导出
        导出选中
        批量转移
        批量删除
        批量编辑
        详情抽屉
      表单详情
        新建车辆
        编辑车辆
        车辆详情
      审批处理
        业务数据
        流程图
        审批历史
        同意
        拒绝
        转办
        加签
    车系统后台
      首页引导
      基础参数
      数据字典
      数据源
      仪表盘配置
      模块组
      模块
      建模
      成员管理
      角色权限
      组织结构
      流程配置
      对外应用
      系统日志
```

## 3. 系统后台全量配置 NS 图

```mermaid
flowchart TD
    A["进入车系统后台<br/>默认超级管理员全量"] --> B["首页引导<br/>配置完成度 / 发布检查 / 下一步"]
    B --> C["基础参数<br/>系统名称 / 图标 / 编码 / 访问地址 / 默认首页"]
    C --> D["数据字典<br/>车辆状态 / 维保类型 / 证件类型"]
    D --> E["数据源<br/>系统内公式 / 外部 API / 数据库直连"]
    E --> F["仪表盘配置<br/>组件库 / 画布 / 属性面板 / 发布"]
    F --> G["模块组<br/>车辆管理"]
    G --> H["模块<br/>车辆档案 / 维保记录 / 驾驶员"]
    H --> I["建模"]

    I --> I1["字段配置<br/>字段名 / 类型 / 必填 / 只读 / 字典 / 搜索 / 筛选"]
    I --> I2["列表配置<br/>默认列 / 宽度 / 固定 / 排序 / 可导出"]
    I --> I3["筛选配置<br/>快捷搜索 / 高级筛选 / 控件 / 操作符"]
    I --> I4["场景配置<br/>全部车辆 / 在用 / 待年检 / 维保中 / 我负责的"]
    I --> I5["页面动作<br/>新建 / 编辑 / 删除 / 转移 / 导入 / 全部导出 / 导出选中 / 批量编辑"]
    I --> I6["菜单发布<br/>绑定模块组 / 运行态菜单 / 发布检查"]

    I1 --> J["发布检查"]
    I2 --> J
    I3 --> J
    I4 --> J
    I5 --> J
    I6 --> J
    J --> K{"检查通过"}
    K -->|否| I
    K -->|是| L["发布到运行态"]

    L --> M["成员管理<br/>邀请成员 / 停用启用 / 分配角色"]
    M --> N["角色权限<br/>模块 / 字段 / 数据 / 动作 / 权限预览"]
    N --> O["组织结构<br/>部门树 / 成员归属 / 数据权限支撑"]
    O --> P["流程配置<br/>绑定模块 / 触发事件 / 审批节点 / 条件 / 抄送 / 外部 API / 发布"]
    P --> Q["对外应用<br/>AppKey / IP 白名单 / 授权范围 / 参数映射 / 调用日志"]
    Q --> R["系统日志<br/>配置日志 / 业务日志 / 流程日志 / 导出审计"]
```

## 4. 运行态全量页面 NS 图

```mermaid
flowchart TD
    A["进入车系统运行态"] --> B["系统仪表盘"]
    B --> B1["统计卡<br/>车辆总数 / 在用 / 待年检 / 维保中"]
    B --> B2["快捷入口<br/>车辆档案 / 维保记录 / 驾驶员"]
    B --> B3["待办与消息<br/>顶栏图标进入"]

    B2 --> C["车辆管理模块组"]
    C --> C1["车辆档案"]
    C --> C2["维保记录"]
    C --> C3["驾驶员"]

    C1 --> D["车辆档案列表"]
    D --> D1["顶部工具栏<br/>场景 / 搜索 / 高级筛选 / 列设置 / 导入 / 导出"]
    D --> D2["表格字段<br/>复选框 / 序号 / 车牌号 / 品牌型号 / 状态 / 部门 / 驾驶员 / 购置日期 / 年检到期 / 最近维保 / 操作"]
    D --> D3["表头能力<br/>排序 / 固定列 / 列宽 / 保存我的表头"]
    D --> D4["行操作<br/>详情 / 编辑 / 更多"]
    D --> D5["勾选后批量条<br/>转移 / 删除 / 导出选中 / 批量编辑 / 清除选择"]
    D --> D6["抽屉<br/>高级筛选 / 列设置 / 详情"]

    D4 --> E["车辆详情"]
    D4 --> F["编辑车辆"]
    D --> G["新建车辆"]
    E --> E1["基础信息<br/>车牌号 / 品牌型号 / 状态 / 部门 / 驾驶员"]
    E --> E2["年检维保<br/>购置日期 / 年检到期 / 最近维保"]
    G --> H{"是否触发审批"}
    F --> H
    H -->|是| I["审批处理页"]
    H -->|否| J["保存完成并写日志"]

    I --> I1["业务数据"]
    I --> I2["流程图"]
    I --> I3["审批历史"]
    I --> I4["操作区<br/>同意 / 拒绝 / 转办 / 加签"]
    I4 --> J
```

## 5. 角色看到的首页和入口

| 角色/账号 | 登录后第一层 | 进入系统后首页 | 可见后台 | 看不到 |
|-----------|--------------|----------------|----------|--------|
| 平台超级管理员 `plat_admin` | 平台工作台 | 可进入已授权系统运行态 | 平台后台、系统后台 | 无，按全量示例展示 |
| 系统管理员 `admin` | 平台工作台 / 我的系统 | 车系统仪表盘 | 系统后台 | 平台后台 |
| 普通成员 `che` | 平台工作台 / 我的系统 | 车系统仪表盘 | 无 | 系统后台、平台后台、建模、角色权限 |

```mermaid
flowchart LR
    A["登录"] --> B{"账号权限"}
    B -->|plat_admin| C["平台工作台"]
    C --> C1["平台后台"]
    C --> C2["我的系统"]
    C2 --> C3["车系统运行态"]
    C3 --> C4["系统后台"]

    B -->|admin| D["我的系统"]
    D --> D1["车系统仪表盘"]
    D1 --> D2["车辆管理"]
    D1 --> D3["系统后台"]
    D -. "不可见" .-> D4["平台后台"]

    B -->|che| E["我的系统"]
    E --> E1["车系统仪表盘"]
    E1 --> E2["车辆档案 / 维保记录 / 驾驶员"]
    E -. "不可见" .-> E3["系统后台"]
    E -. "不可见" .-> E4["平台后台"]
```

## 6. 权限配置如何影响页面

```mermaid
flowchart TD
    A["角色权限配置"] --> B["菜单权限"]
    A --> C["页面动作权限"]
    A --> D["字段权限"]
    A --> E["数据权限"]
    A --> F["OpenAPI 权限"]

    B --> B1["决定左侧菜单和后台入口是否显示"]
    C --> C1["决定新建 / 编辑 / 删除 / 转移 / 导入 / 导出是否显示或禁用"]
    D --> D1["决定列表列 / 表单字段 / 详情字段是否可见可编辑"]
    E --> E1["决定列表能看到哪些数据"]
    F --> F1["决定外部系统能调用哪些模块动作"]

    B1 --> G["运行态最终效果"]
    C1 --> G
    D1 --> G
    E1 --> G
    F1 --> H["对外应用最终效果"]
```

## 7. 超级管理员全量功能清单

### 平台层

| 区域 | 页面 | 全量功能 |
|------|------|----------|
| 登录前 | 登录 | 登录、退出、错误提示、找回入口占位 |
| 登录前 | 注册并创建系统 | 创建平台账号、创建系统、注册人成为系统管理员 |
| 平台工作台 | 我的系统 | 查看系统、进入系统、最近访问、系统状态、我的角色 |
| 平台工作台 | Flow | 平台 Flow 工作台入口、跨系统流程入口 |
| 平台工作台 | 应用 | 平台应用入口、平台能力聚合 |
| 平台工作台 | 日志 | 平台工作台日志查询 |
| 平台顶栏 | 待办/消息 | 待办列表、消息列表、角标提醒 |

### 平台后台

| 页面 | 全量功能 |
|------|----------|
| 概览 | 用户数、系统数、Flow 数、应用数、调用统计 |
| 平台用户 | 新建、启用、停用、搜索、筛选、查看最近登录 |
| 平台角色 | 平台菜单权限、平台用户权限、平台 Flow 权限、平台应用权限、平台日志权限 |
| 平台 Flow | Flow 列表、编码、状态、触发方式、版本、发布、调用情况 |
| 平台应用 | AppKey 脱敏、scope、授权平台 Flow、参数映射、凭证轮换、停用影响 |
| 平台日志 | 操作人、对象、类型、来源、结果、摘要 |
| 全局配置 | 登录安全、OpenAPI 策略、文件存储、配额、功能开关 |

### 系统运行态

| 页面 | 全量功能 |
|------|----------|
| 仪表盘 | 统计卡、快捷入口、待办消息、跳转模块 |
| 车辆档案列表 | 场景、搜索、高级筛选、列设置、导入、全部导出、导出选中、表头排序、序号、勾选、批量操作、详情抽屉 |
| 新建/编辑车辆 | 车牌号、品牌型号、车辆状态、归属部门、驾驶员、购置日期、年检到期、最近维保、备注 |
| 车辆详情 | 基础信息、年检维保、最近操作、编辑入口 |
| 审批处理 | 业务数据、流程图、审批历史、意见、同意、拒绝、转办、加签 |

### 系统后台

| 页面 | 全量功能 |
|------|----------|
| 首页引导 | 配置进度、发布检查、下一步任务 |
| 基础参数 | 系统名称、图标、编码、访问地址、默认首页 |
| 数据字典 | 字典类型、字典项、颜色、状态、排序、字段关联 |
| 数据源 | 系统内公式、外部 API、数据库直连、测试连接、刷新策略 |
| 仪表盘配置 | 组件库、画布、属性面板、数据源绑定、发布 |
| 模块组 | 车辆管理、运行态顶栏排序、状态 |
| 模块 | 车辆档案、维保记录、驾驶员、主字段、数据量、状态 |
| 建模 | 字段、列表、筛选、场景、页面动作、菜单发布、发布检查 |
| 成员管理 | 邀请、停用、启用、移出、分配角色、成员详情 |
| 角色权限 | 模块权限、字段权限、数据权限、动作权限、权限预览、复制角色 |
| 组织结构 | 部门树、成员归属、数据权限支撑 |
| 流程配置 | 绑定模块、触发事件、审批节点、条件、抄送、外部 API、发布检查 |
| 对外应用 | AppKey、Secret 状态、IP 白名单、授权范围、参数映射、调用日志 |
| 系统日志 | 配置变更、业务操作、流程日志、导出审计 |
