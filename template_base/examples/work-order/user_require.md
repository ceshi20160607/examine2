# 工单样例需求

系统用于登记和跟踪内部工单。

管理员可以创建、修改、查看和删除工单。工单包含编号、标题、说明、优先级、是否紧急和创建时间。编号必须唯一，标题必填。

列表默认展示编号、标题、优先级、是否紧急和创建时间。用户可以使用编号或标题快速搜索。点击一行后在右侧查看详情；页面右上角只有一个“新建工单”主按钮。

本样例只验证 Template Base 的生成、构建和真实读写能力，不代表 examine 产品需求。

