# examine2 Frontend 增量规约

- 只为 `state.json` 中 Gate 已接受的当前任务编码；不从本文件推断 coding 开关。
- 当前切片依据需求/设计主包补充最小交互和接口 mapping；粗略原型只作参考，不能复制其四分五裂的功能布局。
- 必须绑定真实 API 和持久化读回；生产路由禁止演示 fixture、接口失败回退假数据和假成功提示。
- 需求、原型或 API 冲突时提交 PM，由提交人 + product + uiux + backend/test 等角色会审。
