# examine2 Test 增量规约

- 每个 task 独立检查 API、DB、UI、Browser、Restart 五类证据；未变化层用累积冒烟或 hash 证明未回退。
- 每个切片从真实角色入口验证可见结果、数据读回、权限正反例、状态、失败和重启；task 通过不能冒充切片通过。
- VS1-VS3 evidence 只证明各自已验收范围，后续切片必须做受影响回归，不能把旧截图当新增能力证据。
- 测试结论必须区分工程任务、角色旅程、发布和用户最终验收。
- 自动化脚本、构建、API 和截图均不能设置 `userAccepted=true`。
