# examine2 Architect 增量规约

- 项目架构基线在 `.cursor/session/rebuild/design-package.md`；当前切片只深化其范围内的数据、接口、权限、状态、错误和运行契约。
- 平台/系统/业务运行/后台配置边界、身份权限、租户数据、流程状态、消息待办日志、副作用和外部集成不得在切片中静默改写。
- 技术栈或跨期架构需要变化时提交 issue 并重新过受影响切片 Gate，不能用全量重设计阻塞无关切片。
- 数据库设计先于生成代码；MyBatis 生成基础层与手写业务层的依赖和覆盖边界必须形成正式契约。
- 发现需求在角色、数据、权限或状态上无法闭环时提交 PM 会审，不在架构里补造产品行为。
