---
description: Harness 自治原型：有需求.md或未 design_delivered 时自动执行 pipeline/runbook L0-L4
alwaysApply: true
---

# hu Prototype Harness

## 触发

- 根目录 `需求.md` 或用户描述产品需求
- 或 `.cursor/state.json` → `gates.design_delivered != true`
- **跳过：** 用户明确「只改代码」且已 design_delivered

## Agent 必须（按序）

1. `.cursor/harness.yaml`
2. `.cursor/feedforward/contract.md`
3. `.cursor/pipeline/runbook.md` — **L0→L4 单会话跑完**
4. `.cursor/feedforward/reference-case.md`
5. `.cursor/feedback/checklist.md` — 传感器
6. `.cursor/feedback/response-contract.md` — 汇报格式
7. `.cursor/feedback/learning-loop.md` — 结束自动执行

兼容别名：`.cursor/skills/rough-to-prototype/SKILL.md` = runbook 副本。

## 用户

只维护 `需求.md`。**禁止**向用户抛选型题；AUTO → `docs/decisions/resolution.md`。

## 交付

100% P0 HTML · `docs/design/prototypes/index.html`
