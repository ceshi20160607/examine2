# config-spec 骨架（L3）

> 标准页必须 deep；其余 P0 写到控件级。

## 1. 业务列表 runtime-list ★

| 区域 | 控件 | 权限 | 显隐 |
|------|------|------|------|
| 页头 | 新建 | create | |
| 工具栏 | 搜索/场景/高级筛选/列设置/导出/导入 | | |
| 选中条 | 批量操作 | | 勾选后 |
| 表 | 序号+勾选 | | |
| 行 | 点击 | view | 右侧详情抽屉 |

密度：1440×900 首屏 ≥8 行。

## 2. 核心配置页 modeling ★

Tab：字段 | 列表 | 筛选 | 场景 | 动作 | 菜单发布  
顶栏：保存草稿 | 发布

## 3. API 控制台（若需求含对外 API）★

| 区域 | 控件 |
|------|------|
| 左 | API 产品列表 |
| 中 | 路由/模块/Flow 绑定 |
| 右 | scope、**字段映射表**、限流、IP |
| 映射列 | 外部参数 | 内部字段 | 转换 | 必填 |

## 4. SSO（若需求含 SSO）★

IdP 类型 | Issuer | Client ID/Secret | 回调 URL | 测试连接 | 强制 SSO

## 5. 租户模式（若需求含多租户）★

单租户/多租户 | 默认租户 | 租户列表（多租户时）

## 6. 其余 P0 页

按 page-inventory 同构补全。
