# 4 小时滚动交付周期

每个周期以 `session/current-node.md` 为唯一当前执行记录，必须包含物理起止时间、一个模块结果、完整/已有/本期/剩余清单、参考桌面 UI 基线、并行 DAG 和唯一周期末包 Gate。

周期内短任务只做聚焦自检，禁止各自运行全量回归、重启、浏览器矩阵或打包。周期末滚动包归属于 CP1/CP2/CP3 中当前正式里程碑，但不新增正式检查点，也不改变正式检查点状态。
